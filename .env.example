# Credentials belong in the operating-system keyring.
# MYYOLO_PASSWORD is an optional automation fallback; do not commit it.
# One profile keeps separate mySIGN and admin sessions in that keyring.
MYYOLO_DB_PATH=./data/myyolo.sqlite
